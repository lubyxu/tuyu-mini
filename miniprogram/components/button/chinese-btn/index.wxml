<!--components/button/chinese-btn/index.wxml-->
<view class="chinese-style-btn {{type === 'default' ? 'primary-1' : ''}} {{className}}" style="width: {{width}}{{unit}}; z-index: {{zIndex}}" bind:tap="onClick">
  <slot></slot>
</view>