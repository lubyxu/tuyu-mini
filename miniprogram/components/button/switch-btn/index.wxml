<view bind:tap="onSwitch" class="switch-btn {{checked ? 'switch-btn-checked' : 'switch-btn-unchecked'}}">
  <view class="pin"></view>
	<view class="spin-text">{{unCheckText}}</view>
  <view class="track"></view>
</view>