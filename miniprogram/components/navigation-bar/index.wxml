
<cover-view class="{{ title ?  'nav-bar-with-title' : 'nav-bar'}} {{className}}" style="height:{{navBarHeight}}px;">
    <cover-view
        wx-if="{{title}}"
        class="title-bar-title"
        style="line-height:{{menuHeight}}px;bottom:{{menuBotton}}px;">{{title}}</cover-view>
    <cover-image
        wx-if="{{showHome}}"
        class="icon"
        src="../../images/icons/home.png"
        style="line-height:{{menuHeight}}px;left:{{menuRight}}px;bottom:{{menuBotton}}px;"
        bind:tap="switchTab"
    />
    <cover-image
        wx-if="{{showBack}}"
        class="return-big-icon"
        src="https://fuyuoss.oss-cn-shanghai.aliyuncs.com/front-end/return-icon.png"
        style="line-height:{{menuHeight}}px;left:{{menuRight}}px;bottom:{{menuBotton}}px;"
        bind:tap="navigateBack"
    />
</cover-view>

