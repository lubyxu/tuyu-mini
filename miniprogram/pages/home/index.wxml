<!--index.wxml-->
<view>
  <loading wx-if="{{showLoading}}" />
  <view wx-if="{{!showLoading}}" class="page-section swiper">
  <view class="swiper-box">
    <cover-view class="position-box">
      <button
        catchtouchmove="preventD"
        wx:if="{{!isLogined}}"
        open-type="getPhoneNumber"
        bindgetphonenumber="onRegisterAccount"
        hover-class="none"
        style="width: auto;"
      >
        <cover-image
          src="https://fuyuoss.oss-cn-shanghai.aliyuncs.com/front-end/not-login.png"
          class="home-icon"
        />
      </button>
      <cover-image
        src="{{avatar}}"
        class="home-icon"
        bindtap="gotoProtral"
        wx:else
      />
      <cover-image
          src="https://fuyuoss.oss-cn-shanghai.aliyuncs.com/front-end/position.png"
          class="positon-icon"
      />
      <cover-view class="position-text">{{position}}</cover-view>
    </cover-view>
    <swiper
      indicator-dots="{{indicatorDots}}"
      autoplay="{{autoplay}}"
      interval="{{interval}}"
      duration="{{duration}}"
      style="height: 190px;"
    >
      <block wx:for="{{banners}}" wx:key="*this">
        <swiper-item>
          <view class="banner-img-wrap relative" data-index="{{index}}" bindtap="bannerClick">
            <image
              src="{{item.url}}"
              class="banner-img"
              mode="widthFix"
  
            />
            <button
              wx:if="{{!isLogined}}"
              data-id="h5"
              data-index="{{index}}"
              catchtouchmove="preventD"
              open-type="getPhoneNumber"
              bindgetphonenumber="onRegisterAccount"
              hover-class="none"
              style="width: auto;"
              class="absolute l-0 t-0 r-0 b-0"
            >
            </button>
          </view>
        </swiper-item>
      </block>
    </swiper>
  </view>

  <view class="nav-container">
    <view class="nav-container-box">
      <view wx:for="{{selectList}}" wx:key="{{item.index}}" data-id="{{item.index}}" bindtap="switchTab" class="{{selected === index ? 'nav-box-active' : 'nav-box'}}">
        <image class="nav-icon" src="{{selected === index ? item.selectedIconPath : item.iconPath}}"></image>
        <view class="{{selected === index ? 'tab-bar-text-active' : 'tab-bar-text'}}">{{item.text}}</view>
      </view>
    </view>
  </view>

    <view class="scroll-view-container" wx:if="{{selected === 1}}">
      <cardList list="{{list}}" bind:scrollBottom="scrollBottom" bind:ocrClick="ocrClick" bind:onFind="onFind"></cardList>
    </view>
    <view class="scroll-view-container" wx:else>
      <path-notes bind:onLoginSuccess="onLoginSuccess" />
    </view>
  </view>
</view>
