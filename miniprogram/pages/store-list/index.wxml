<!--index.wxml-->
<view>
  <loading wx-if="{{showLoading}}" />
  <view wx-if="{{!showLoading}}" class="page">
    <navigation-bar default-data="{{defaultData}}"></navigation-bar>
    <store-product appid="{{appid}}" product-id="{{productId}}"></store-product>
  </view>
</view>
